// script.js

// Remove the alert when the page loads
window.onload = function() {
    // alert("Welcome to the Protected Area Management System!"); // This line is removed
};

// Example function to handle navigation (to be developed further)
function navigateTo(page) {
    window.location.href = page;
}
